# API key for securing your API (required)
# Generate with: openssl rand -hex 32
API_KEY=your-secret-api-key-here

# Port for local development (optional)
# Railway sets this automatically in production - DON'T set this in Railway!
# Locally, defaults to 3000 if not specified
PORT=3000

# Google Service Account credentials (optional - for Drive uploads)
# Paste the entire JSON key file contents as a single line
GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

# Google Drive folder ID where reports will be uploaded (optional)
# Get this from the folder URL: drive.google.com/drive/folders/FOLDER_ID_HERE
GOOGLE_DRIVE_FOLDER_ID=

# Node environment
NODE_ENV=production
